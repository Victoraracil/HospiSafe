<UserControl x:Class="HospiSafe.Views.CitasView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:i="http://schemas.microsoft.com/xaml/behaviors" xmlns:local="clr-namespace:HospiSafe.ViewModels" xmlns:converters="clr-namespace:HospiSafe.Converters" mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <converters:EstadoColorConverter x:Key="EstadoColorConverter"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <local:CitasViewModel/>
    </UserControl.DataContext>
    <Grid Background="{StaticResource Gray50}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding CargarCommand}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers><!-- ================= HEADER ================= -->
        <Border Grid.Row="0" Background="White" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Gestión de Citas" FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Nueva Cita" Padding="12,6" Margin="0,0,10,0" Background="{StaticResource PrimaryColor}" Foreground="White" BorderThickness="0" Command="{Binding LimpiarCommand}"/>
                </StackPanel>
            </Grid>
        </Border><!-- ================= CONTENIDO ================= -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions><!-- ========= LISTADO DE CITAS ========= -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" BorderBrush="#e2e8f0" BorderThickness="1" Padding="20" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Listado de Citas" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListView Grid.Row="1" ItemsSource="{Binding ListaCitas}" SelectedItem="{Binding CitaSeleccionada}" BorderThickness="0" HorizontalContentAlignment="Stretch">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#f8fafc" CornerRadius="10" Padding="12" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                                            <TextBlock FontSize="14" FontWeight="SemiBold">
                                                <Run Text="{Binding Paciente.Nombre}"/>
                                                <Run Text="{Binding Paciente.Apellidos}"/>
                                            </TextBlock>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Dr/a. " FontSize="12" Foreground="#64748b"/>
                                                <TextBlock FontSize="12" Foreground="#64748b">
                                                    <Run Text="{Binding Usuario.Nombre}"/>
                                                    <Run Text="{Binding Usuario.Apellidos}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>
                                        <Border Grid.Column="1" Background="{Binding Estado, Converter={StaticResource EstadoColorConverter}}" CornerRadius="4" Padding="6,2" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Estado}" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border><!-- ========= FORMULARIO ========= -->
            <Border Grid.Column="1" Background="White" CornerRadius="12" BorderBrush="#e2e8f0" BorderThickness="1" Padding="25">
                <StackPanel>
                    <TextBlock Text="Detalles de la Cita" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions><!-- Fecha -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                            <TextBlock Text="Fecha"/>
                            <DatePicker SelectedDate="{Binding Fecha}" Height="34"/>
                        </StackPanel><!-- Estado -->
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                            <TextBlock Text="Estado"/>
                            <ComboBox ItemsSource="{Binding EstadosCita}" SelectedItem="{Binding Estado}" Height="34"/>
                        </StackPanel><!-- Paciente -->
                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="5">
                            <TextBlock Text="Paciente"/>
                            <ComboBox ItemsSource="{Binding ListaPacientes}" SelectedItem="{Binding PacienteSeleccionado}" Height="34">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Run Text="{Binding DNI}"/>
                                            <Run Text=" - "/>
                                            <Run Text="{Binding Nombre}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding Apellidos}"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel><!-- Doctor/Usuario -->
                        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="5">
                            <TextBlock Text="Doctor/a Asignado/a"/>
                            <ComboBox ItemsSource="{Binding ListaUsuarios}" SelectedItem="{Binding UsuarioSeleccionado}" Height="34">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Run Text="{Binding Nombre}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding Apellidos}"/>
                                            <Run Text=" ("/>
                                            <Run Text="{Binding Rol}"/>
                                            <Run Text=")"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </Grid><!-- BOTONES -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Guardar Cita" Command="{Binding GuardarCommand}" Background="{StaticResource PrimaryColor}" Foreground="White" Margin="0,0,10,0"/>
                        <Button Content="Eliminar Cita" Command="{Binding EliminarCommand}" Background="#ef4444" Foreground="White" Margin="0,0,10,0"/>
                        <Button Content="Limpiar" Command="{Binding LimpiarCommand}" Background="White" BorderBrush="#cbd5e1" BorderThickness="1"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>