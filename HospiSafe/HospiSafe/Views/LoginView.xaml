<Window x:Class="HospiSafe.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HospiSafe.Views"
        mc:Ignorable="d"
        Title="Login" 
        Height="450" 
        Width="400" 
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">

            <!-- styles para deshabilitar cuando loading a true -->
            <StackPanel.Resources>
                <Style TargetType="TextBox">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>

                <Style TargetType="PasswordBox">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>

                <Style TargetType="Button">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Resources>

            <TextBlock Text="HospiSafe" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Correo Electrónico" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Correo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Padding="5"/>
            <TextBlock Text="Contraseña" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Margin="0,0,0,10" Padding="5"/>

            <!-- progressbar visible -->
            <ProgressBar IsIndeterminate="True" Height="6" Margin="0,0,0,10"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>

            <!-- mensaje de error -->
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10" TextWrapping="Wrap"/>

            <Button Height="35" Padding="10,0" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Content" Value="Iniciar Sesión"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Content" Value="Cargando..."/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>

</Window>
