<UserControl x:Class="HospiSafe.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HospiSafe.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" 
             d:DesignWidth="1000" 
             Background="{StaticResource Gray50}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="+" FontSize="24" Foreground="{StaticResource PrimaryLightColor}" FontWeight="Bold" Margin="0,0,8,0"/>
                    <TextBlock Text="HospiSafe" FontSize="20" Foreground="{StaticResource PrimaryColor}" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource PrimaryLightColor}" Width="40" Height="40" CornerRadius="20" Margin="0,0,10,0">
                        <TextBlock Text="{Binding UserInitial}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                        <TextBlock Text="{Binding UserName}" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="{Binding UserRole}" Foreground="#64748b" FontSize="12"/>
                    </StackPanel>
                    <Button Content="Salir" Background="Transparent" Foreground="#64748b" BorderBrush="#cbd5e1" BorderThickness="1" Command="{Binding LogoutCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>

            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" Padding="30">
            <StackPanel MaxWidth="1200" HorizontalAlignment="Center">
                <Border Background="{StaticResource PrimaryColor}" CornerRadius="15" Padding="30" Margin="0,0,0,30">
                    <StackPanel>
                        <TextBlock Text="Bienvenido a HospiSafe" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Seleccione un módulo para comenzar a trabajar." Foreground="White" Opacity="0.9" FontSize="16"/>
                    </StackPanel>
                </Border>
                <ItemsControl ItemsSource="{Binding Modules}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Width="250" Height="250" Margin="10,20" Background="White" Command="{Binding DataContext.OpenModuleCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="12" Padding="20">
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <Border Width="70" Height="70" Background="#f8fafc" CornerRadius="35" Margin="0,0,0,15">
                                                    <TextBlock Text="{Binding Icon}" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource PrimaryColor}"/>
                                                </Border>
                                                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#0f172a" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                                <TextBlock Text="{Binding Description}" FontSize="13" Foreground="#64748b" TextWrapping="Wrap" TextAlignment="Center"/>
                                                <Border Background="#dcfce7" CornerRadius="10" Padding="8,2" HorizontalAlignment="Center" Margin="0,15,0,0">
                                                    <TextBlock Text="Activo" FontSize="11" FontWeight="Bold" Foreground="#166534"/>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
